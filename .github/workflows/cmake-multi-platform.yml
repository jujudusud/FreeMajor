# FreeMajor CMake Multi-Platform - FIXED Windows MSYS2 COMPILER ISSUE
name: FreeMajor CMake Multi-Platform

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]
  release:
    types: [ published, prereleased ]

jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            name: "Linux"
            build_type: Release
            
          - os: macos-latest
            name: "macOS"
            build_type: Release
            
          - os: windows-latest
            name: "Windows"
            build_type: Release

    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      with:
        submodules: recursive

    - name: Set reusable strings
      id: strings
      shell: bash
      run: |
        echo "build-output-dir=${{ github.workspace }}/build" >> "$GITHUB_OUTPUT"

    # === LINUX ===
    - name: Install Linux dependencies
      if: matrix.os == 'ubuntu-latest'
      run: |
        sudo apt-get update
        sudo apt-get install -y \
          cmake \
          build-essential \
          libasound2-dev \
          libjack-jackd2-dev \
          libfltk1.3-dev \
          gettext \
          git

    # === macOS ===
    - name: Install macOS dependencies
      if: matrix.os == 'macos-latest'
      run: |
        brew install cmake fltk gettext

    # === Windows - FIXED with NINJA ===
    - name: Setup MSYS2 (Windows)
      if: matrix.os == 'windows-latest'
      uses: msys2/setup-msys2@v2
      with:
        msystem: MINGW64
        path-type: minimal
        install: >-
          mingw-w64-x86_64-cmake
          mingw-w64-x86_64-ninja
          mingw-w64-x86_64-fltk
          mingw-w64-x86_64-gettext
          mingw-w64-x86_64-gcc
          mingw-w64-x86_64-cc
          git
        update: true

    # Configure Linux/macOS
    - name: Configure CMake (Linux/macOS)
      if: matrix.os != 'windows-latest'
      run: |
        cmake -B ${{ steps.strings.outputs.build-output-dir }} \
          -DCMAKE_BUILD_TYPE=${{ matrix.build_type }} \
          -S ${{ github.workspace }}

    # FIXED: Windows uses NINJA generator (MSYS2 recommended)
    - name: Configure CMake (Windows)
      if: matrix.os == 'windows-latest'
      shell: msys2 {0}
      run: |
        SOURCE_DIR=$(pwd)
        mkdir -p build
        cd build
        cmake "$SOURCE_DIR" \
          -G Ninja \
          -DCMAKE_BUILD_TYPE=${{ matrix.build_type }} \
          -DCMAKE_C_COMPILER=gcc \
          -DCMAKE_CXX_COMPILER=g++

    # Build Linux/macOS
    - name: Build (Linux/macOS)
      if: matrix.os != 'windows-latest'
      run: cmake --build ${{ steps.strings.outputs.build-output-dir }} --config ${{ matrix.build_type }}

    # Build Windows with NINJA
    - name: Build (Windows)
      if: matrix.os == 'windows-latest'
      shell: msys2 {0}
      working-directory: ${{ github.workspace }}/build
      run: cmake --build . --config ${{ matrix.build_type }}

    # Debug output
    - name: List build results
      if: always()
      run: find ${{ steps.strings.outputs.build-output-dir }} -name "*FreeMajor*" -type f 2>/dev/null || true
      shell: bash

    # Archive releases only
    - name: Package Linux/macOS
      if: startsWith(github.ref, 'refs/tags/') && 
          (contains(github.ref_name, 'nightly') || contains(github.ref_name, 'pre') || startsWith(github.ref_name, 'v'))
      shell: bash
      run: |
        cd ${{ steps.strings.outputs.build-output-dir }}
        find . -name "*FreeMajor*" -type f -executable | head -5 | xargs tar -czf FreeMajor-${{ matrix.name }}.tar.gz || true

    - name: Upload Linux/macOS
      if: startsWith(github.ref, 'refs/tags/') && 
          (contains(github.ref_name, 'nightly') || contains(github.ref_name, 'pre') || startsWith(github.ref_name, 'v')) &&
          matrix.os != 'windows-latest'
      uses: actions/upload-artifact@v4
      with:
        name: FreeMajor-${{ matrix.name }}
        path: ${{ steps.strings.outputs.build-output-dir }}/*.tar.gz

    - name: Package Windows
      if: startsWith(github.ref, 'refs/tags/') && 
          (contains(github.ref_name, 'nightly') || contains(github.ref_name, 'pre') || startsWith(github.ref_name, 'v')) &&
          matrix.os == 'windows-latest'
      shell: msys2 {0}
      working-directory: ${{ github.workspace }}/build
      run: |
        find . -name "*FreeMajor*" -type f -executable | head -5 | xargs tar -czf FreeMajor-Windows.tar.gz || true

    - name: Upload Windows
      if: startsWith(github.ref, 'refs/tags/') && 
          (contains(github.ref_name, 'nightly') || contains(github.ref_name, 'pre') || startsWith(github.ref_name, 'v')) &&
          matrix.os == 'windows-latest'
      uses: actions/upload-artifact@v4
      with:
        name: FreeMajor-Windows
        path: ${{ github.workspace }}/build/*.tar.gz
