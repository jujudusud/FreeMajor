# FreeMajor CMake Multi-Platform - FIXED Windows MinGW issue
name: FreeMajor CMake Multi-Platform

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]
  release:
    types: [ published, prereleased ]

jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            name: "Linux"
            build_type: Release
            
          - os: macos-latest
            name: "macOS"
            build_type: Release
            
          - os: windows-latest
            name: "Windows"
            build_type: Release

    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      with:
        submodules: recursive

    - name: Set reusable strings
      id: strings
      shell: bash
      run: |
        echo "build-output-dir=${{ github.workspace }}/build" >> "$GITHUB_OUTPUT"

    # === LINUX ===
    - name: Install Linux dependencies
      if: matrix.os == 'ubuntu-latest'
      run: |
        sudo apt-get update
        sudo apt-get install -y \
          cmake \
          build-essential \
          libasound2-dev \
          libjack-jackd2-dev \
          libfltk1.3-dev \
          gettext \
          git

    # === macOS ===
    - name: Install macOS dependencies
      if: matrix.os == 'macos-latest'
      run: |
        brew install cmake fltk gettext

    # === Windows - FIXED ===
    - name: Setup MSYS2 (Windows)
      if: matrix.os == 'windows-latest'
      uses: msys2/setup-msys2@v2
      with:
        msystem: MINGW64
        install: >-
          mingw-w64-x86_64-cmake
          mingw-w64-x86_64-fltk
          mingw-w64-x86_64-gettext
          mingw-w64-x86_64-gcc
          mingw-w64-x86_64-make
          git
        update: true
        path-type: minimal

    # Configure Linux/macOS
    - name: Configure CMake (Linux/macOS)
      if: matrix.os != 'windows-latest'
      run: |
        cmake -B ${{ steps.strings.outputs.build-output-dir }} \
          -DCMAKE_BUILD_TYPE=${{ matrix.build_type }} \
          -S ${{ github.workspace }}

    # Configure Windows - CORRECTED generator
    - name: Configure CMake (Windows)
      if: matrix.os == 'windows-latest'
      shell: msys2 {0}
      run: |
        cd ${{ github.workspace }}
        mkdir -p build
        cd build
        cmake .. \
          -G "MSYS Makefiles" \
          -DCMAKE_BUILD_TYPE=${{ matrix.build_type }}

    # Build Linux/macOS
    - name: Build (Linux/macOS)
      if: matrix.os != 'windows-latest'
      run: cmake --build ${{ steps.strings.outputs.build-output-dir }} --config ${{ matrix.build_type }}

    # Build Windows - CORRECTED shell
    - name: Build (Windows)
      if: matrix.os == 'windows-latest'
      shell: msys2 {0}
      working-directory: ${{ github.workspace }}/build
      run: cmake --build . --config ${{ matrix.build_type }}

    # Archive only for releases/nightly/pre-releases
    - name: Package and Upload Linux/macOS
      if: startsWith(github.ref, 'refs/tags/') && 
          (contains(github.ref_name, 'nightly') || 
           contains(github.ref_name, 'pre') || 
           startsWith(github.ref_name, 'v')) &&
          matrix.os != 'windows-latest'
      shell: bash
      run: |
        cd ${{ steps.strings.outputs.build-output-dir }}
        tar -czf FreeMajor-${{ matrix.name }}.tar.gz FreeMajor FreeMajor.exe */FreeMajor */FreeMajor.exe || true
        ls -la *.tar.gz
      # Upload
    - name: Upload Linux/macOS binaries
      if: startsWith(github.ref, 'refs/tags/') && 
          (contains(github.ref_name, 'nightly') || 
           contains(github.ref_name, 'pre') || 
           startsWith(github.ref_name, 'v')) &&
          matrix.os != 'windows-latest'
      uses: actions/upload-artifact@v4
      with:
        name: FreeMajor-${{ matrix.name }}
        path: ${{ steps.strings.outputs.build-output-dir }}/*.tar.gz
        retention-days: 90

    - name: Upload Windows binaries
      if: startsWith(github.ref, 'refs/tags/') && 
          (contains(github.ref_name, 'nightly') || 
           contains(github.ref_name, 'pre') || 
           startsWith(github.ref_name, 'v')) &&
          matrix.os == 'windows-latest'
      shell: msys2 {0}
      run: |
        cd ${{ github.workspace }}/build
        ls -la FreeMajor* *.exe */FreeMajor* */*.exe
        # Package Windows binaries
        tar -czf FreeMajor-Windows.tar.gz FreeMajor*.exe || true
      # Upload
    - name: Upload Windows artifact
      if: startsWith(github.ref, 'refs/tags/') && 
          (contains(github.ref_name, 'nightly') || 
           contains(github.ref_name, 'pre') || 
           startsWith(github.ref_name, 'v')) &&
          matrix.os == 'windows-latest'
      uses: actions/upload-artifact@v4
      with:
        name: FreeMajor-Windows
        path: ${{ github.workspace }}/build/*.tar.gz
        retention-days: 90

    - name: Show build results
      run: find ${{ steps.strings.outputs.build-output-dir }} -name "FreeMajor*" -type f
